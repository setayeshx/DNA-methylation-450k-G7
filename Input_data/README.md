# Input_data

Place the raw data here before running the pipeline:

- `*.idat` — 16 files (Red + Green channel, 2 per sample × 8 samples)
- `SampleSheet_Report_II.csv` — sample metadata

These files are **not tracked in git** (see `.gitignore`). They are large
binaries and can be re-downloaded from the course materials / GEO.
